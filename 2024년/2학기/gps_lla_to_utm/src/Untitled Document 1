/*
#include"stdio.h"

int main(void)
{
    int temp, result;
    FILE* fp;

    // 파일 열기
    fp = fopen("/home/amap/2024_onefifth_catkin_ws/src/Satellite/mapviz/mapviz/launch/mapviz_shark.launch", "r");

    if(fp == NULL)
    {
        printf("파일이 없습니다\n\n");
        return -1;
    }

    else
    {
        // 파일 내용 읽기
        for(int i = 0; i < 20; i++)
        {
            result = fscanf(fp, "%d", &temp);

            if(result == EOF)
            {
                break;
            }
            else
            {
                 printf("%d\n",temp);
            }
        }
    }
    fclose(fp);

    return 0;
}
*/

